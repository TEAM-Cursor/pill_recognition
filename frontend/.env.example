# frontend/.env.example — 값이 필요한 것만. 실제 값은 frontend/.env 에(커밋 금지).
# 사용법: 이 파일을 .env 로 복사한 뒤 필요하면 값을 바꾼다.

# 백엔드 API 베이스 URL. 미설정 시 코드 기본값 http://localhost:8000 사용.
# 실기기(휴대폰)에서 테스트하려면 PC의 LAN IP 로 바꾼다. 예: http://192.168.0.10:8000
# VITE_API_BASE=http://localhost:8000
